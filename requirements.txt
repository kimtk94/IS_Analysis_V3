# Install `synapseclient` to download production manifest entries through the
# authenticated `synapse` CLI. Fixture tests use only the Python standard library.
# Downstream scientific commands are optional and documented in config/defaults.yaml.
synapseclient
